import { IsDoubleDirective } from './is-double.directive';

describe('IsDoubleDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDoubleDirective();
    expect(directive).toBeTruthy();
  });
});
